{
  "name": "applynest",
  "private": true,
  "description": "ApplyNest monorepo — client (Vite/React) and API (Express)",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev -w @applynest/web",
    "dev:api": "npm run dev -w @applynest/api",
    "build": "npm run build -w @applynest/web && npm run build -w @applynest/api",
    "typecheck": "npm run typecheck -w @applynest/web --if-present && npm run typecheck -w @applynest/api --if-present",
    "lint": "npm run lint -w @applynest/web --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
