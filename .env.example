# Copy to apps/api/.env for local API development (never commit real secrets).

# API server
PORT=4000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# --- Add as features are implemented (examples) ---
# MONGODB_URI=
# JWT_SECRET=

# --- Optional: client production build ---
# VITE_API_URL=https://your-api.example.com
